import "dart:convert"; import "dart:io"; void main() async { print("í·ª CodeHer Backend Integration Test Suite"); print("=" * 50); try { final client = HttpClient(); final request = await client.getUrl(Uri.parse("http://localhost:5000/")); final response = await request.close(); final responseBody = await response.transform(utf8.decoder).join(); if (response.statusCode == 200) { final data = jsonDecode(responseBody); print("âœ… Backend is running: ${data["message"]}"); print("   API Version: ${data["version"]}"); } client.close(); } catch (e) { print("âŒ Test failed: $e"); } }
